"use strict";
const crypto=require("crypto");class ActionStore{constructor(){this.actions=new Map();}create(actor,action,payload={},ttl=300000){const id=crypto.randomBytes(6).toString("base64url");this.actions.set(id,{actor:String(actor),action,payload,expiresAt:Date.now()+ttl});return{id,expiresAt:new Date(Date.now()+ttl).toISOString()};}resolve(actor,id){const row=this.actions.get(String(id));if(!row||row.expiresAt<Date.now()){this.actions.delete(String(id));throw Object.assign(new Error("Esta confirmação expirou."),{code:"CARDINAL_ACTION_EXPIRED"});}if(row.actor!==String(actor))throw Object.assign(new Error("Acesso negado."),{code:"CARDINAL_PERMISSION_DENIED"});this.actions.delete(String(id));return row;}}module.exports={ActionStore};

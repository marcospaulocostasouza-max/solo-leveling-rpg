"use strict";
const path = require("path"); const { CardinalOperationsService } = require("./service"); const { Rbac } = require("../admin/rbac"); const { TaskStore } = require("../developer/task-store"); const { GitManager } = require("../developer/git/manager"); const { KnowledgeIndexer } = require("../knowledge");
function createOperationsService(options = {}) { const root = path.resolve(options.root || process.cwd()), database = options.database || require("../../packages/database"), rbac = options.rbac || new Rbac({ database, ownerNumber: options.ownerNumber }), developerStore = options.developerStore || new TaskStore(options.developerStatePath || path.join(root, "cardinal/cache/developer-tasks.json")), git = options.git || new GitManager(root, options.worktreeRoot || path.join(root, ".cardinal-worktrees")); return new CardinalOperationsService({ ...options, root, database, developerStore, git, authorize: options.authorize || ((actor, permission) => rbac.authorize(actor, permission)), reindex: options.reindex || (() => new KnowledgeIndexer({ projectRoot: root }).rebuild()) }); }
module.exports = { createOperationsService };

'use client';
import { useState } from 'react';
export default function AdminDungeons(){const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function submit(form:FormData){setBusy(true);setMessage('');try{const body={name:form.get('name'),rank:form.get('rank'),gateType:form.get('gateType'),x:Number(form.get('x')),y:Number(form.get('y')),rewardWon:Number(form.get('rewardWon')||0),rewardXp:Number(form.get('rewardXp')||0),boss:form.get('boss')||null,locationHint:form.get('locationHint')||null};const r=await fetch('/api/admin/dungeons',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error);setMessage(`Dungeon criada: ${d.id}`)}catch(e){setMessage(e instanceof Error?e.message:'Erro ao criar Dungeon.')}finally{setBusy(false)}}return <main className="login-page"><section className="login-card"><small>ADMINISTRAÇÃO</small><h1>Criar Dungeon semanal</h1><p>Criação manual; spawn aleatório permanece desativado.</p><form action={submit}><label><span>Nome</span><input name="name" required/></label><label><span>Rank</span><input name="rank" required defaultValue="E"/></label><label><span>Tipo</span><select name="gateType"><option value="common">Comum</option><option value="red">Vermelho</option></select></label><label><span>Coordenada X (0–100)</span><input name="x" type="number" min="0" max="100" step="0.01" required/></label><label><span>Coordenada Y (0–100)</span><input name="y" type="number" min="0" max="100" step="0.01" required/></label><label><span>Won</span><input name="rewardWon" type="number" min="0" defaultValue="0"/></label><label><span>XP</span><input name="rewardXp" type="number" min="0" defaultValue="0"/></label><label><span>Boss conhecido</span><input name="boss"/></label><label><span>Região aproximada</span><input name="locationHint"/></label><button disabled={busy}>{busy?'CRIANDO...':'CRIAR DUNGEON'}</button></form>{message&&<em>{message}</em>}</section></main>}

"use strict";
const PERMISSIONS = Object.freeze({ knowledge: "CARDINAL_READ", web: "CARDINAL_WEB_SEARCH", forge: "CARDINAL_FORGE", approval: "CARDINAL_READ", publisher: "CARDINAL_PUBLISH", admin: "CARDINAL_PLAYER_ADMIN", developer: "CARDINAL_DEVELOPER", operations: "CARDINAL_OPS_DEPLOY", verify: "CARDINAL_READ", balance: "CARDINAL_BALANCE_RUN", narrative: "CARDINAL_NARRATIVE_CREATE", world: "CARDINAL_WORLD_EVENTS" });
class PolicyEngine { constructor(config) { this.config = config; } permission(task) { return PERMISSIONS[task.type]; } approvalRequired(task, preferences = {}) { if (task.type === "approval") return true; if (task.risk === "CRITICAL") return true; if (task.type === "publisher") return preferences.requirePublicationApproval ?? this.config.requirePublicationApproval; if (task.type === "operations" && task.action === "deploy_change") return preferences.requireDeployApproval ?? this.config.requireDeployApproval; return false; } canRetry(error) { return new Set(["TIMEOUT", "ECONNREFUSED", "MODEL_OFFLINE", "SERVICE_UNAVAILABLE", "DATABASE_OFFLINE"]).has(error?.code); } assertScope(plan) { if (/internet|email|ssh|outro computador|arquivos pessoais|comprar servico/i.test(plan.goal)) throw Object.assign(new Error("Objetivo fora do projeto RPG."), { code: "SCOPE_BLOCKED" }); } }
module.exports = { PolicyEngine, PERMISSIONS };

"use strict";
const { CardinalAssistant } = require("../core"); const { KnowledgeRetriever } = require("../knowledge"); const { CardinalForgeService } = require("../forge"); const { createAdminService } = require("../admin"); const { createDeveloperService } = require("../developer"); const { createOperationsService } = require("../operations"); const { WorldDirector } = require("../world"); const { BalanceEngine } = require("../balance"); const { createNarrativeService } = require("../narrative"); const { WebResearch } = require("../web"); const { Rbac } = require("../admin/rbac"); const { CardinalOrchestratorService } = require("./service");
function createOrchestratorService(options = {}) { const database = options.database || require("../../packages/database"), rbac = options.rbac || new Rbac({ database, ownerNumber: options.ownerNumber }), assistant = options.assistant || new CardinalAssistant(options), knowledge = options.knowledge || new KnowledgeRetriever(options), forge = options.forge || new CardinalForgeService({ ...options, client: assistant.client }), admin = options.admin || createAdminService({ ...options, database }), developer = options.developer || createDeveloperService({ ...options, database }), operations = options.operations || createOperationsService({ ...options, database }), world = options.world || new WorldDirector({ ...options, database }), balance = options.balance || admin.balance || new BalanceEngine({ ...options, database }), narrative = options.narrative || createNarrativeService({ ...options, database, world, knowledge }), web = options.web || new WebResearch(options); return new CardinalOrchestratorService({ ...options, modules: { knowledge, web, forge, admin, developer, operations, world, balance, narrative }, authorize: options.authorize || ((actor, permission) => rbac.authorize(actor, permission)), identity: options.identity || (actor => rbac.identity(actor)) }); }
module.exports = { createOrchestratorService };

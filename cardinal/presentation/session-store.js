"use strict";
const crypto=require("crypto");class SessionStore{constructor(options={}){this.ttl=options.ttl||86400000;this.sessions=new Map();}get(actor){const key=String(actor);let row=this.sessions.get(key);if(!row||row.expiresAt<Date.now()){row={session_id:`crd_${crypto.randomBytes(8).toString("hex")}`,actor:key,history:[],context:{},expiresAt:Date.now()+this.ttl};this.sessions.set(key,row);}return row;}update(actor,patch){const row=this.get(actor);Object.assign(row.context,patch);row.expiresAt=Date.now()+this.ttl;return row;}add(actor,message){const row=this.get(actor);row.history.push({...message,at:new Date().toISOString()});row.history=row.history.slice(-30);return row;}}module.exports={SessionStore};

# Verificação PostgreSQL

Pendente de `DATABASE_URL` estar disponível no processo. Após executar `npm run db:migrate`, rode
`npm run db:verify` e registre aqui contagens, divergências e amostras de jogadores, inventário,
Won, equipamentos, técnicas, localização e memórias NPC.
